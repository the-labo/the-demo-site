/**
 * Test for urlUtil.
 * Runs with mocha.
 */
'use strict'

const urlUtil = require('../utils/urlUtil')
const { ok, equal } = require('assert')

describe('url-util', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
