/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

const Scene = require('./Scene')
const cn = require('./concerns')

const class {{name}}Base = cn.compose(

)(Scene)

/** @lends {{name}} */
class {{name}} extends Scene {
  get scope () {
    return this.store.{{scopePath}}
  }
}

module.exports = {{name}}
