/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

const Scene = require('./Scene')
const {compose, withBusy} =  require('the-scene-mixins/shim')

const class {{name}}Base = compose(

)(Scene)

/** @lends {{name}} */
class {{name}} extends Scene {
  get scope () {
    return this.store.{{scopePath}}
  }
}

module.exports = {{name}}
