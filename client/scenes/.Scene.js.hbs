/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

const Scene = require('./Scene')
const cn = require('./concerns')

/** @lends {{name}} */
const {{name}} = cn.compose(
)(
  class {{name}}Base extends Scene {
    get scope () {
      const s = this
      return s.store.{{scopePath}}
    }
  }
)

module.exports = {{name}}
