'use strict'

const path = require('path')

module.exports = [

].map((name) => ({
  force: false,
  mkdirp: true,
  mode: '644',
  path: `${name}.js`,
  data: {
    name: path.basename(name)
  }
}))

if (!module.parent) {
  require('coz').render(__filename)
}

