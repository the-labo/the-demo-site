'use strict'

const {dir} = require('the-templates')

module.exports = dir({
  dirname: __dirname,
  name: 'abstract',
  cjs: true,
  description: 'Abstract scopes'
})

if (!module.parent) {
  require('coz').render(__filename)
}