/**
 * Bud file for fragment components
 */
'use strict'

const {Bound} = require('the-templates')

module.exports = [
  'user/UserPager',
  'caution/CautionDisconnectedDialog',
].map((name) => Bound({
  name
}))

if (!module.parent) {
  require('coz').render(__filename)
}

