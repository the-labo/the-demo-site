/**
 * Bud file for fragment components
 */
'use strict'

const {Bound} = require('the-templates')

module.exports = [
].map((name) => Bound({
  name
}))

if (!module.parent) {
  require('coz').render(__filename)
}

