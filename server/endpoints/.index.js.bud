'use strict'

const {dir} = require('the-templates')

module.exports = dir({
  dirname: __dirname,
  node: true,
  name: 'endpoints',
  description: 'HTTP endpoints'
})

if (!module.parent) {
  require('coz').render(__filename)
}