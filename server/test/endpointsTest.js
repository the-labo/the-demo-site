/**
 * Test for endpoints.
 * Runs with mocha.
 */
'use strict'

const endpoints = require('../endpoints')
const { ok, equal } = require('assert')

describe('endpoints', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
