/**
 * Test for db.
 * Runs with mocha.
 */
'use strict'

const db = require('../db')
const { ok, equal } = require('assert')

describe('db', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
