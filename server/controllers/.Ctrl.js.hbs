/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

const Ctrl = require('./Ctrl')
const cn = require('./concerns')

/** @lends {{name}} */
const {{name}} = cn.compose(
  cn.withDebug
)(
  class {{name}}Base extends Ctrl {
  }
  return {{name}}Base
)

module.exports = {{name}}
