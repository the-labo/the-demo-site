/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

const {TheCtrl} = require('the-controller-base')
const {withDebug} = require('./concerns')

/** @lends {{name}} */
class {{name}} extends TheCtrl {
}

module.exports = withDebug(
  {{name}}
)
