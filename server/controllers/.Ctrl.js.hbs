/**
 * {{name}}
 * @class {{name}}
 */
'use strict'

const {TheCtrl} = require('the-controller-base')
const cn = require('./concerns')

/** @lends {{name}} */
const {{name}} = cn.compose(
  cn.withDebug
)(
  class {{name}}Base extends TheCtrl {
 }
)

module.exports = {{name}}
