{
  "default": {
    "DATABASE": "thedemosite_dev",
    "DIALECT": "mysql",
    "HOST": "127.0.0.1",
    "PASSWORD": "thedemosite_dev",
    "PORT": "#{Local.MYSQL_CONTAINER_PORT}",
    "ROOT_PASSWORD": "root",
    "ROOT_USERNAME": "root",
    "USERNAME": "thedemosite_dev"
  },
  "production": {
    "DATABASE": "#{Local.DB_SCHEMA}",
    "DIALECT": "mysql",
    "HOST": "#{Local.DB_HOST}",
    "PASSWORD": "#{Local.DB_PASSWORD}",
    "PORT": "#{Local.DB_PORT}",
    "ROOT_PASSWORD": "#{Local.DB_ROOT_USER}",
    "ROOT_USERNAME": "#{Local.DB_ROOT_PASSWORD}",
    "USERNAME": "#{Local.DB_USER}"
  },
  "test": {
    "DIALECT": "sqlite",
    "STORAGE": "var/storage/the-demo-site.db"
  }
}