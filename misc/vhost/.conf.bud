'use strict'

const {APP_DOMAIN: domain, APP_PORT, port} = require('@self/Local')

module.exports = {
  force: true,
  mode: '444',
  path: `${domain}.conf`,
  data: {
    domain,
    port,
    root: __dirname
  }
}

if (!module.parent) {
  require('coz').render(__filename)
}